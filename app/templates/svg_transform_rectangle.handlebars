<g xmlns="http://www.w3.org/2000/svg">
	<rect class="transform-rect" x="0" y="0" fill="none" stroke="{{strokeColor}}" stroke-width="{{strokeSize}}" stroke-miterlimit="{{strokeSize}}" width="200" height="200"/>
	<rect class="ul-scale-handle" x="-15" y="-15" fill="{{fillColor}}" stroke="{{strokeColor}}" stroke-width="{{strokeSize}}" stroke-miterlimit="{{strokeSize}}" width="{{handleSize}}" height="{{handleSize}}"/>
	<rect class="ur-scale-handle" x="185" y="-15" fill="{{fillColor}}" stroke="{{strokeColor}}" stroke-width="{{strokeSize}}" stroke-miterlimit="{{strokeSize}}" width="{{handleSize}}" height="{{handleSize}}"/>
	<rect class="ll-scale-handle" x="-15" y="185" fill="{{fillColor}}" stroke="{{strokeColor}}" stroke-width="{{strokeSize}}" stroke-miterlimit="{{strokeSize}}" width="{{handleSize}}" height="{{handleSize}}"/>
	<rect class="lr-scale-handle" x="185" y="185" fill="{{fillColor}}" stroke="{{strokeColor}}" stroke-width="{{strokeSize}}" stroke-miterlimit="{{strokeSize}}" width="{{handleSize}}" height="{{handleSize}}"/>
	<g class="rotation-group">
		<circle class="rotate-handle" fill="{{fillColor}}" stroke="{{strokeColor}}" stroke-width="{{strokeSize}}" stroke-miterlimit="{{strokeSize}}" cx="0" cy="{{rotationHandleYOffset}}" r="{{rotationHandleRadius}}"/>
		<line fill="none" stroke="{{strokeColor}}" stroke-width="{{strokeSize}}" stroke-miterlimit="{{strokeSize}}" x1="0" y1="-40" x2="0" y2="0"/>
	</g>
</g>